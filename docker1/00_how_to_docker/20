docker swarm init --advertise-addr $(docker-machine ip Char)

# swarm init - создает узел
# --advertise-addr - делает менеджером.
# docker node ls - список узлов
# docker info - в блоке swarm можно посмотреть инфу