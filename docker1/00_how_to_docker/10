docker volume create --name hatchery

# том (volume) - папки вне контейнеров, для сохранения data, можно подключать к контейнерам.
# инфа из томов не удаляется без нашего запроса. посмотреть список томов - docker volume ls